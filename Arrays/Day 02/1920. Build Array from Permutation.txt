class Solution {
    public int[] buildArray(int[] nums) {
        int[] rt = new int[nums.length];
        for(int i =0; i <nums.length;i++){
            rt[i]=nums[nums[i]];
        }
        return rt;
    }
}